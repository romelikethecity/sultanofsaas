---
phase: 04-newsletter-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/rome/Documents/projects/newsletters/worker/subscribe.js
  - /Users/rome/Documents/projects/sultanofsaas/scripts/templates.py
  - /Users/rome/Documents/projects/sultanofsaas/assets/css/styles.css
autonomous: false
requirements: [NEWS-01, NEWS-02, NEWS-03, NEWS-04]

must_haves:
  truths:
    - "Visitor sees a branded newsletter signup section on every page type"
    - "Signup form submits to D1 worker without CORS errors"
    - "Submitted email appears in D1 under sultan-of-saas list"
    - "Form uses gold/dark SultanOfSaaS brand styling, not generic colors"
  artifacts:
    - path: "/Users/rome/Documents/projects/newsletters/worker/subscribe.js"
      provides: "CORS allowed origins including sultanofsaas.com"
      contains: "sultanofsaas.com"
    - path: "/Users/rome/Documents/projects/sultanofsaas/scripts/templates.py"
      provides: "Newsletter signup HTML/JS embedded in get_page_wrapper()"
      contains: "nl-section"
    - path: "/Users/rome/Documents/projects/sultanofsaas/assets/css/styles.css"
      provides: "Newsletter signup styles matching brand"
      contains: "nl-section"
  key_links:
    - from: "scripts/templates.py (newsletter form)"
      to: "https://newsletter-subscribe.rome-workers.workers.dev/subscribe"
      via: "fetch POST in inline JS"
      pattern: "newsletter-subscribe.*rome-workers"
    - from: "newsletter form data-list"
      to: "D1 sultan-of-saas list"
      via: "data-list attribute value"
      pattern: 'data-list="sultan-of-saas"'

user_setup:
  - service: cloudflare-d1
    why: "Seed new newsletter list in D1 database and deploy updated worker"
    env_vars: []
    dashboard_config:
      - task: "Run wrangler d1 execute to seed sultan-of-saas list, then wrangler deploy"
        location: "Terminal with wrangler auth (cd /Users/rome/Documents/projects/newsletters/worker/)"
---

<objective>
Add email newsletter capture to every page on SultanOfSaaS using the existing D1 newsletter worker infrastructure.

Purpose: Capture visitor emails before scaling content in Phases 5-9. Every page should have a branded "Sultan's Pick" newsletter signup section.
Output: Newsletter signup form on all 334+ pages, CORS configured, D1 list seeded.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/templates.py
@assets/css/tokens.css
@assets/css/styles.css

<interfaces>
<!-- From templates.py — the key function where newsletter form will be injected -->
From scripts/templates.py:
```python
def get_page_wrapper(title, description, canonical_path, body_content, active_page="", extra_head=""):
    """Wrap body content in a complete HTML page."""
    # Returns: head + nav + <main>{body_content}</main> + footer + scripts
```

From scripts/templates.py:
```python
def get_footer_html():
    """Generate multi-column footer."""
    # Returns: <footer class="site-footer">...</footer>
```

<!-- From tokens.css — brand variables the newsletter form must use -->
From assets/css/tokens.css:
```css
--color-primary: #D4A054;            /* Warm gold */
--color-primary-light: #E8C078;      /* Gold highlight */
--color-primary-subtle: rgba(212, 160, 84, 0.12); /* Gold tint bg */
--bg-primary: #0C0E12;               /* Page background */
--bg-secondary: #12151B;             /* Card background */
--bg-tertiary: #1A1D25;              /* Elevated surface */
--border-subtle: rgba(255, 255, 255, 0.06);
--border-gold: rgba(212, 160, 84, 0.30);
--text-primary: #F0F0F0;
--text-secondary: #9CA3AF;
--text-tertiary: #6B7280;
--font-display: 'Outfit', sans-serif;
--font-body: 'Source Serif 4', Georgia, serif;
--radius-md: 10px;
--radius-lg: 14px;
--shadow-gold: 0 0 0 1px rgba(212, 160, 84, 0.3), 0 4px 24px rgba(212, 160, 84, 0.1);
```

<!-- From newsletter embed template — the form structure to adapt -->
From /Users/rome/Documents/projects/newsletters/embed/signup-form.html:
```html
<div class="nl-signup" id="nl-signup">
  <form id="nl-form" data-list="REPLACE-WITH-LIST-SLUG">
    <input type="email" name="email" placeholder="Your email" required class="nl-input">
    <button type="submit" class="nl-btn">Subscribe Free</button>
  </form>
  <p class="nl-msg" id="nl-msg" style="display:none"></p>
  <p class="nl-fine">No spam. Unsubscribe anytime.</p>
</div>
```

<!-- Worker API_URL — use the worker URL directly, not the newsletters.getprovyx.com alias -->
Worker endpoint: https://newsletter-subscribe.rome-workers.workers.dev

<!-- From subscribe.js — ALLOWED_ORIGINS array (lines 17-36) where sultanofsaas.com must be added -->
From /Users/rome/Documents/projects/newsletters/worker/subscribe.js:
```javascript
const ALLOWED_ORIGINS = [
  'https://b2bsalestools.com',
  'https://www.b2bsalestools.com',
  // ... other origins ...
  'http://localhost:3000',
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CORS origins + newsletter form component + brand styles</name>
  <files>
    /Users/rome/Documents/projects/newsletters/worker/subscribe.js
    /Users/rome/Documents/projects/sultanofsaas/scripts/templates.py
    /Users/rome/Documents/projects/sultanofsaas/assets/css/styles.css
  </files>
  <action>
Three changes in this task:

**A. CORS origins (subscribe.js)**

Edit `/Users/rome/Documents/projects/newsletters/worker/subscribe.js` line 17-36. Add these two entries to the ALLOWED_ORIGINS array (before the localhost entries):
```
'https://sultanofsaas.com',
'https://www.sultanofsaas.com',
```

Do NOT redeploy the worker — the user will do that manually after verifying. Just save the file.

**B. Newsletter signup section (templates.py)**

Add a new function `get_newsletter_html()` to `scripts/templates.py` that returns a full-width newsletter signup section. Place the function between `get_footer_html()` and `get_page_wrapper()`.

The HTML structure:
```html
<section class="nl-section">
  <div class="container">
    <div class="nl-card">
      <div class="nl-content">
        <h3 class="nl-title">The Sultan's Pick</h3>
        <p class="nl-desc">One email per week. The best SaaS tool I reviewed, why it won, and who should buy it.</p>
      </div>
      <form class="nl-form" id="nl-form" data-list="sultan-of-saas">
        <div class="nl-form-row">
          <input type="email" name="email" placeholder="you@company.com" required class="nl-input" id="nl-email">
          <button type="submit" class="nl-btn">Subscribe Free</button>
        </div>
        <p class="nl-msg" id="nl-msg"></p>
        <p class="nl-fine">No spam. Unsubscribe anytime.</p>
      </form>
    </div>
  </div>
</section>
```

Key details:
- `data-list="sultan-of-saas"` (hardcoded, not a parameter)
- API_URL = `https://newsletter-subscribe.rome-workers.workers.dev`
- Use unique IDs: `nl-form`, `nl-msg`, `nl-email`

Then modify `get_page_wrapper()` to insert `{get_newsletter_html()}` between the closing `</main>` tag and `{get_footer_html()}`. The newsletter section sits between main content and footer on every page.

Add inline JS for the form submission inside `get_newsletter_html()` return value (inside a `<script>` tag within the section). The JS should:
- Listen for form submit
- POST to `https://newsletter-subscribe.rome-workers.workers.dev/subscribe` with JSON body `{email, list}`
- On success: hide form, show "You're in. Watch your inbox." in green
- On error: show error message in red, re-enable button
- On network error: show "Connection error. Try again." in red
- Disable button and show "Subscribing..." during request
- Use double curly braces `{{` in f-string Python context for JS braces

**C. Brand-matched styles (styles.css)**

Append newsletter signup styles to the end of `assets/css/styles.css`. The form must feel native to the SultanOfSaaS dark/gold theme. Use CSS variables from tokens.css exclusively — no hardcoded colors.

Style specifications:
- `.nl-section`: full-width, `padding: var(--space-16) 0`, `border-top: 1px solid var(--border-subtle)`, `background: var(--bg-secondary)`
- `.nl-card`: `background: var(--bg-tertiary)`, `border: 1px solid var(--border-gold)`, `border-radius: var(--radius-lg)`, `padding: var(--space-12) var(--space-8)`, `box-shadow: var(--shadow-gold)`, `text-align: center`, `max-width: 640px`, `margin: 0 auto`
- `.nl-title`: `font-family: var(--font-display)`, `font-size: var(--text-3xl)`, `font-weight: 700`, `color: var(--color-primary)`, `margin-bottom: var(--space-2)`
- `.nl-desc`: `font-family: var(--font-body)`, `color: var(--text-secondary)`, `font-size: var(--text-base)`, `margin-bottom: var(--space-6)`, `line-height: 1.6`
- `.nl-form-row`: `display: flex`, `gap: var(--space-2)`, `max-width: 440px`, `margin: 0 auto`
- `.nl-input`: `flex: 1`, `padding: 12px 16px`, `background: var(--bg-primary)`, `border: 1px solid var(--border-default)`, `border-radius: var(--radius-md)`, `color: var(--text-primary)`, `font-family: var(--font-body)`, `font-size: var(--text-sm)`, `outline: none`, `transition: border-color var(--transition-default)`
- `.nl-input::placeholder`: `color: var(--text-tertiary)`
- `.nl-input:focus`: `border-color: var(--color-primary)`, `box-shadow: 0 0 0 2px var(--color-primary-subtle)`
- `.nl-btn`: `padding: 12px 24px`, `background: var(--color-primary)`, `color: var(--text-inverse)`, `border: none`, `border-radius: var(--radius-md)`, `font-family: var(--font-display)`, `font-weight: 600`, `font-size: var(--text-sm)`, `cursor: pointer`, `white-space: nowrap`, `transition: background var(--transition-default)`
- `.nl-btn:hover`: `background: var(--color-primary-light)`
- `.nl-btn:disabled`: `opacity: 0.6`, `cursor: not-allowed`
- `.nl-msg`: `font-size: var(--text-sm)`, `margin-top: var(--space-2)`, `min-height: 20px` (always present in DOM, empty by default)
- `.nl-msg.success`: `color: var(--verdict-excellent)`
- `.nl-msg.error`: `color: var(--verdict-poor)`
- `.nl-fine`: `font-size: var(--text-xs)`, `color: var(--text-tertiary)`, `margin-top: var(--space-2)`
- Responsive: at `max-width: 480px`, `.nl-form-row` becomes `flex-direction: column`, `.nl-btn` gets `width: 100%`
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/sultanofsaas && python3 scripts/build.py && python3 scripts/audit.py && grep -c "nl-section\|nl-form" output/index.html && grep -c "sultanofsaas.com" /Users/rome/Documents/projects/newsletters/worker/subscribe.js</automated>
  </verify>
  <done>
    - subscribe.js ALLOWED_ORIGINS contains sultanofsaas.com and www.sultanofsaas.com
    - templates.py has get_newsletter_html() function with sultan-of-saas list slug
    - get_page_wrapper() includes newsletter section between main and footer
    - styles.css has branded newsletter styles using CSS variables from tokens.css
    - build.py produces 334+ pages, each containing the newsletter form HTML
    - audit.py passes with 0 errors
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Seed D1 list + deploy worker</name>
  <files>D1 database (remote), Cloudflare Worker (remote)</files>
  <action>
User must run two commands that require Cloudflare wrangler authentication (Claude cannot authenticate):

1. Seed the sultan-of-saas list in D1
2. Deploy the updated worker with new CORS origins
3. Verify the list exists in D1

Commands are provided in how-to-verify below.
  </action>
  <what-built>Newsletter form is embedded on all pages and CORS origins are updated in subscribe.js. Two manual steps remain to make the form functional end-to-end.</what-built>
  <how-to-verify>
    1. Seed the sultan-of-saas list in D1:
       ```
       cd /Users/rome/Documents/projects/newsletters/worker
       npx wrangler d1 execute newsletter-subscribers --remote --command="INSERT INTO lists (slug, name, from_email, created_at) VALUES ('sultan-of-saas', 'The Sultan''s Pick', 'insights@sultanofsaas.com', datetime('now'));"
       ```
    2. Deploy the updated worker with new CORS origins:
       ```
       cd /Users/rome/Documents/projects/newsletters/worker
       npx wrangler deploy
       ```
    3. Verify the list was seeded:
       ```
       npx wrangler d1 execute newsletter-subscribers --remote --command="SELECT * FROM lists WHERE slug='sultan-of-saas';"
       ```
  </how-to-verify>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://newsletter-subscribe.rome-workers.workers.dev/health</automated>
  </verify>
  <done>
    - D1 lists table contains sultan-of-saas row
    - Worker is deployed with sultanofsaas.com in ALLOWED_ORIGINS
    - /health endpoint returns 200
  </done>
  <resume-signal>Type "done" after D1 list is seeded and worker is deployed</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification</name>
  <files>output/ (all generated pages)</files>
  <action>
User visually inspects the newsletter form across page types and tests a real email submission end-to-end.
  </action>
  <what-built>Newsletter signup form on all SultanOfSaaS pages, posting to D1 worker</what-built>
  <how-to-verify>
    1. Start local preview:
       ```
       cd /Users/rome/Documents/projects/sultanofsaas/output && python3 -m http.server 8086
       ```
    2. Visit http://localhost:8086/ — verify gold-themed newsletter card appears between content and footer
    3. Check several page types:
       - Tool review: http://localhost:8086/reviews/hubspot/
       - Category: http://localhost:8086/best/crm/
       - Comparison: http://localhost:8086/hubspot-vs-salesforce/
       - Pricing: http://localhost:8086/pricing/hubspot/
       - Niche: http://localhost:8086/best/crm/for-startups/
       - Industry: http://localhost:8086/industry/saas/
       - Stack: http://localhost:8086/stacks/smb-founder/
    4. Test form submission with a real email (after Task 2 is done):
       - Enter email, click Subscribe Free
       - Should show success message in green
       - Check D1 for the subscriber record
    5. Test responsive: resize browser to mobile width, form should stack vertically
    6. Verify the card has gold border, gold heading, dark background — matches site brand
  </how-to-verify>
  <verify>
    <automated>cd /Users/rome/Documents/projects/sultanofsaas && grep -l "nl-form" output/index.html output/reviews/hubspot/index.html output/best/crm/index.html output/pricing/hubspot/index.html | wc -l</automated>
  </verify>
  <done>
    - Newsletter form visible on all page types
    - Gold/dark brand styling confirmed
    - Form submission works end-to-end (email appears in D1)
    - Responsive layout confirmed on mobile width
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` completes without errors
- `python3 scripts/audit.py` returns 0 errors
- Every page in output/ contains "nl-form" and "sultan-of-saas" in its HTML
- subscribe.js contains both sultanofsaas.com origins
- Newsletter section is visually positioned between main content and footer
- Form submission to D1 worker returns success (after CORS deploy)
</verification>

<success_criteria>
1. All 334+ generated pages contain the newsletter signup form
2. Form posts to the existing D1 worker at newsletter-subscribe.rome-workers.workers.dev
3. CORS allows requests from sultanofsaas.com
4. D1 has sultan-of-saas list seeded
5. Signup form uses gold/dark brand styling with no generic colors
6. Audit passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-newsletter-capture/04-01-SUMMARY.md`
</output>
