---
phase: 02-content-remediation
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - scripts/build.py
autonomous: true
requirements: [FIX-04, FIX-05, FIX-06]

must_haves:
  truths:
    - "Every niche page picks a winner different from the main category Sultan's Pick"
    - "All 17 industry pages have picks for all 9 categories"
    - "5 index pages (tools, stacks, best, about, for) have BreadcrumbList JSON-LD schema"
    - "Running python3 scripts/audit.py produces zero ERROR-level findings"
  artifacts:
    - path: "scripts/build.py"
      provides: "Fixed niche rankings, complete industry picks, BreadcrumbList on index pages"
  key_links:
    - from: "scripts/build.py"
      to: "scripts/audit.py"
      via: "audit imports TOOLS, CATEGORIES, NICHES, INDUSTRIES from build"
      pattern: "from build import"
---

<objective>
Fix all data integrity issues (niche winner conflicts, missing industry picks) and SEO errors (missing BreadcrumbList schema), then rebuild the site and run the full audit to confirm zero errors.

Purpose: Resolve the remaining 12 non-writing errors (7 niche conflicts + 5 missing BreadcrumbList) and verify the entire audit passes clean after all 3 plans are complete.
Output: Clean build.py data + full audit passing with zero errors.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-automated-quality-audit/01-01-SUMMARY.md
@.planning/phases/01-automated-quality-audit/01-02-SUMMARY.md
@scripts/audit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix niche winner conflicts and add BreadcrumbList to index pages</name>
  <files>scripts/build.py</files>
  <action>
**Niche winner conflicts (7 errors, NICHES data starting ~line 1384):**

Each niche has a `rankings` dict mapping category slugs to ordered lists of tool slugs. The first tool in each list is the niche winner. The audit flags when this winner matches the Sultan's Pick for that category.

Conflicts to fix:
1. solo-founders / ai-sdr: winner 'lavender' = Sultan's Pick. Move lavender to #2, promote the current #2 tool to #1. Add a why_winners entry explaining why the new #1 suits solo founders.
2. small-teams / sales-engagement: winner 'apollo' = Sultan's Pick. Swap apollo to #2.
3. free / ai-sdr: winner 'lavender' = Sultan's Pick. Swap lavender to #2.
4. free / sales-engagement: winner 'apollo' = Sultan's Pick. Swap apollo to #2.
5. agencies / data-enrichment: winner 'clay' = Sultan's Pick. Swap clay to #2.
6. startups / ai-sdr: winner 'lavender' = Sultan's Pick. Swap lavender to #2.
7. startups / sales-engagement: winner 'apollo' = Sultan's Pick. Swap apollo to #2.

For each conflict: look at the tools in that niche's ranking for the category. Pick the #2 tool as the new winner (swap positions). The niche page should recommend a different winner than the main category page -- that's the whole point of niche-specific recommendations. Update or add a why_winners entry with a brief (1-2 sentence) rationale for why this tool suits the niche better.

**Industry completeness (FIX-06):**
Run the audit check for industry_missing_category. If any industry is missing picks for any of the 9 categories, add them. Based on Phase 1 audit, there were 0 industry completeness errors -- but verify and fix if any exist.

**BreadcrumbList on index pages (5 SEO errors):**
The following 5 index pages are missing BreadcrumbList JSON-LD schema:
- tools/index.html
- stacks/index.html
- best/index.html
- about/index.html
- for/index.html

Find the build functions that generate these pages (build_tools_index, build_stacks_index/build_stack_pages, build_about_page, and the function generating best/ and for/ pages). Add BreadcrumbList JSON-LD in the same pattern used by other pages. For index pages, the breadcrumb should be: Home > Section Name.

Look at how existing pages generate BreadcrumbList (e.g., in build_tool_pages or build_category_pages) and follow the same pattern.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/sultanofsaas && python3 -c "
import sys; sys.path.insert(0, 'scripts')
from audit import check_data
errors = [f for f in check_data() if f.severity == 'error']
for f in errors:
    print(f'{f.entity}/{f.section}: {f.message}')
print(f'Data errors: {len(errors)}')
sys.exit(1 if errors else 0)
"</automated>
  </verify>
  <done>Zero niche_winner_conflict errors. Zero industry_missing_category errors. BreadcrumbList schema added to 5 index page generators.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild site and run full audit -- zero errors required</name>
  <files>scripts/build.py</files>
  <action>
This task depends on Plans 01 and 02 being complete (all writing fixes done).

1. Rebuild the site: `python3 scripts/build.py`
2. Run the full audit: `python3 scripts/audit.py`
3. If any ERROR-level findings remain, fix them. The most likely sources of remaining errors:
   - Missed banned words in content files (re-check with audit output)
   - Missed em-dashes in build.py data
   - BreadcrumbList not rendering correctly on rebuilt pages

4. Re-run audit until zero errors. Warnings are acceptable (they're nice-to-have, not blockers).

**Expected final state:** `python3 scripts/audit.py` exits with code 0 and reports "PASSED with N warnings" (no errors).

If errors persist after initial rebuild, identify which file contains the violation from the audit output (entity field shows tool slug, niche:slug, or industry:slug) and fix inline. Do NOT modify audit.py to suppress errors.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/sultanofsaas && python3 scripts/build.py > /dev/null 2>&1 && python3 scripts/audit.py 2>&1 | tail -5</automated>
  </verify>
  <done>python3 scripts/audit.py exits with code 0. Zero ERROR-level findings. Site rebuilds successfully with 334+ pages.</done>
</task>

</tasks>

<verification>
Full audit produces zero errors:
```
python3 scripts/build.py && python3 scripts/audit.py
```
Exit code 0. Report shows "PASSED with N warnings" -- no errors in writing, structure, data, or seo categories.
</verification>

<success_criteria>
- All 7 niche winner conflicts resolved (different winner than Sultan's Pick)
- All 17 industries have picks for all 9 categories
- 5 index pages have BreadcrumbList JSON-LD schema
- Full audit: zero errors across all categories
- Site builds successfully with 334+ pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-remediation/02-03-SUMMARY.md`
</output>
